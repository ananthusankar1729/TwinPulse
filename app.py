"""
TwinPulse - Streamlit Dashboard (PLACEHOLDER)
===============================================

This file is intentionally left as a placeholder for now.

Stage 1 of this project (this current stage) only builds:
  - the project foundation, and
  - the synthetic production data generator (src/data_generator.py)

The interactive dashboard (floor supervisor / plant manager / leadership
views) and any ML-based sensor inference / defect prediction will be
built in later stages, on top of data/production_data.csv.

To generate the data this app will eventually use, run:
    python src/data_generator.py
"""

if __name__ == "__main__":
    print(
        "TwinPulse dashboard is not built yet.\n"
        "This is Stage 1: project foundation + synthetic data generator.\n"
        "Run `python src/data_generator.py` to generate data/production_data.csv, "
        "then `python notebooks/validate_dataset.py` to verify it."
    )
